/**
 * DetectLoop
 */
/*
 * approach - slow and fast pointer approach
 * if the slow pointer at any point becomes equal to fast pointer
 * the linked list contains loop
 * else it does not
 */
public class DetectLoop {

    public static void main(String[] args) {
        // Node slow = head;
        // Node fast = head.next;
        // while(fast != null && fast.next != null) {
        //     if(slow == fast) {
        //         return true;
        //     }
        //     slow = slow.next;
        //     fast = fast.next.next;
        // }
        // return false;
    }
}
